name: Deploy API Gateway
on:
  push:
    branches:
      - main
    paths:
      - "gcp/apigateway/mysticparking/openapispec.yaml"

jobs:
  deploy-apigateway-mysticparking:
    runs-on: ubuntu-latest
    steps:
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d-%H-%M-%S')"

      - uses: actions/checkout@v3

      - id: auth
        uses: google-github-actions/auth@v0
        with:
          credentials_json: "${{ secrets.gcp_credentials }}"

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v0"

      - id: "create_config"
        run: >
          gcloud
          api-gateway
          api-configs
          create
          mysticparking-config-${{ steps.date.outputs.date }}
          --api=mysticparking
          --openapi-spec=gcp/apigateway/mysticparking/openapispec.yaml
          --project=twin-villas
      - id: "deploy_config"
        run: >
          gcloud
          api-gateway
          gateways
          update
          mysticparkinggateway
          --api=mysticparking
          --api-config=mysticparking-config-${{ steps.date.outputs.date }}
          --location=us-east4
          --project=twin-villas
